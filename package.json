{
  "name": "@mykytapimonovtd/hugo-llm-mirror",
  "version": "1.0.1",
  "description": "Generates LLM training data from Hugo documentation",
  "license": "UNLICENSED",
  "files": [
    "dist"
  ],
  "bin": {
    "hugo-llm-mirror": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsup src/cli.ts --format cjs --clean",
    "prepare": "npm run build"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "dependencies": {
    "minimatch": "^10.2.5"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "tsup": "^8.5.1",
    "typescript": "^6.0.3"
  }
}